On Linux machine:
sudo apt-get install stockfish
To run stockfish as multiple processes cp /usr/games/stockfish to stockfish-stuff folder in project directory
To run stockfish in multiple threads cp /usr/games/stockfish project directory